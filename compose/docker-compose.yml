version: '3'
services:
  my_client_1:
    image: yashgt/client
    build: ../client
    #restart: on-failure:3    
    volumes:
    - serverlog:/log
    depends_on:
    - my_server    
    labels:
        - "constraint:node==gol05854"    
    networks:
    - mynet        
  my_server:
    image: yashgt/proc1
    #image: gophernet/netcat
    #command: -v -l -p 5000    
    expose:
     - "5000"    
    build: ../proc1
    #restart: on-failure:3    
    volumes:
    - serverlog:/log
    labels:
        - "constraint:node==gol05854"    
    networks:
    - mynet        
volumes:
    serverlog:
         driver: local
networks:
    mynet:
        driver: overlay    
